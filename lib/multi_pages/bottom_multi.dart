import 'package:flutter/material.dart';

class BottomMulti extends StatelessWidget {
  const BottomMulti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}