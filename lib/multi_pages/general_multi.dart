import 'package:flutter/material.dart';

class GeneralMulti extends StatelessWidget {
  const GeneralMulti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}