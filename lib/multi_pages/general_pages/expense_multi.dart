import 'package:flutter/material.dart';

class ExpenseMulti extends StatelessWidget {
  const ExpenseMulti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}