import 'package:flutter/material.dart';

class InventoryMulti extends StatelessWidget {
  const InventoryMulti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}