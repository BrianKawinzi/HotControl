import 'package:flutter/material.dart';

class ProfileMulti extends StatelessWidget {
  const ProfileMulti({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}